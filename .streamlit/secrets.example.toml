# Copiar este contenido en Streamlit Cloud > App > Settings > Secrets
# MODEL_URL solo hace falta si dev/modelo.pt NO está dentro del repositorio.

MODEL_URL = "https://drive.google.com/uc?export=download&id=TU_FILE_ID"

# Opcional: si querés usar Google Maps para la geocodificación inversa.
# Si lo dejás vacío o no lo cargás, la app usa OpenStreetMap/Nominatim sin API key.
GOOGLE_MAPS_API_KEY = "TU_API_KEY_DE_GOOGLE_MAPS"
